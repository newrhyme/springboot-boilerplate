spring:
  config:
    activate:
      on-profile: local-mysql

  datasource:
    url: jdbc:mysql://localhost:3306/boilerplate?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: "boilerplate"
    password: "000624"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: true

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG

app:
  profile-name: local-mysql